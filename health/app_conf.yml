version: 1
scheduler:
  period_sec: 20
requests:
  timeout_sec: 5
  host: kafka-lab6.canadacentral.cloudapp.azure.com
  receiver: /receiver/health
  storage: /storage/health
  processing: /processing/health
  audit: /audit/health
